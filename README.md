# Workspace Configs

Stores config files and info for setting up development workspaces.


## Development Environments:
### Python
Use pipenv to manage virtual environments for different projects.

PIPENV_VENV_IN_PROJECT is set to true (.zshenv). This conflicts with pipenv-pipes.

